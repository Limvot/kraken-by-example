import io:*

fun main():int {
    // This is an example of a line comment
    // Notice how there are two slashes at the beginning of the line
    // And that nothing written inside these will be read by the compiler

    // println("Hello, world!");

    // Run it. See? Now try deleting the two slashes, and run it again.

    /* This is a block comment! Kraken supports these too. */

    println("I know about comments!") // Maybe this code needs a note about it

     return 0
}
